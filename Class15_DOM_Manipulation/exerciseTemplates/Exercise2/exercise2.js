/**
 * Function to change the text of each paragraph.
 */
function changeTexts() {
    // Get each paragraph element by ID

    // Change the text content of each paragraph to something different
}

// Call the function
changeTexts();
